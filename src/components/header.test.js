import React from "react";
import { render, screen } from "@testing-library/react";
import Header from "../components/Header"; // Make sure the file path is correct

test("renders a <header> element", () => {
  // Your test code here
});
