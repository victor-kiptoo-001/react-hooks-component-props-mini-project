import React from "react";
import { render, screen } from "@testing-library/react";
import About from "../components/About"; // Make sure the file path is correct

test("renders an <aside> element", () => {
  // Your test code here
});
